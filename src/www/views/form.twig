<form action="{{ path('trim_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_label(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <input type="submit" />
</form>
